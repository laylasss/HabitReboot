<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.layla.habitreboot">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomePage1Activity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AppUsageActivity"></activity>
        <activity android:name=".HomePage2Activity"></activity>
        <activity android:name=".HomePage3Activity"></activity>
        <activity android:name=".AdvanceActivity"></activity>
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".TodolistActivity"></activity>
        <activity android:name=".CreateNewActivity"></activity>
        <activity android:name=".CreateActivityNavigation1"></activity>
        <activity android:name=".CreateGameActivityDaily"></activity>
        <activity android:name=".CreateGameActivityQuit"></activity>
        <activity android:name=".CreateGameActivityWeekly"></activity>
        <activity android:name=".CreateWaterActivity"></activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>